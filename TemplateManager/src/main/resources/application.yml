server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/alert
    username: root
    password: root1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true